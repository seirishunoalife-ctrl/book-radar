# ホスティング検討: Google Cloud Platform (GCP) Always Free Tierでの固定IP確保

調査日: 2026-07-25(当初はOracle Cloud Infrastructureで検討していたが、アカウント作成時に原因不明のエラーが繰り返し発生し断念。GCP Always Free Tierに切り替えて再検討)

## 1. 背景

楽天ブックスAPIはIP許可リストが必須(book-metadata-research.md 4章)。Renderのような動的IPのPaaSでは継続利用できないため、固定の送信元IPを持つ常時稼働サーバーが必要。Google Books APIへの切り替えは精度検証の結果、非推奨と結論済み(book-metadata-research.md 6〜8章)。

個人開発・低コストの原則(引き継ぎ資料10章)から、まずOracle Cloud Infrastructure (OCI) Always Free Tierを検討したが、ユーザー環境(PC・スマホ両方)でアカウント作成時にエラーが繰り返し発生したため断念。**Google Cloud Platform (GCP) Always Free Tier**に切り替えて同様の構成を検討する。

## 2. GCP Always Free Tierの仕様(公式ドキュメント確認済み)

| 項目 | 内容 |
|---|---|
| e2-microインスタンス | **月1台まで無料**(non-preemptible)。0.25 vCPU(バースト時最大2 vCPU)・メモリ1GB |
| 利用可能リージョン | **`us-west1`(オレゴン)/ `us-central1`(アイオワ)/ `us-east1`(サウスカロライナ)の米国3リージョンのみ**。日本リージョンは無料枠対象外 |
| ストレージ | 標準永続ディスク(pd-standard)30GB-月まで無料 |
| 静的外部IPアドレス | **実行中のVMインスタンスに割り当てている間は無料**。停止中のインスタンスに割り当てたまま、または未使用で予約したままだと時間課金が発生する。→ **VMは常時稼働させ、停止しない運用を徹底する必要がある**(OCIのEphemeral IPと異なり、GCPは動かし続けることが無料条件) |
| 送信データ転送 | 北米→全リージョン(中国・オーストラリア除く)向けに月1GBまで無料。日本の楽天/カーリルAPIへのAPIリクエスト(小さなJSON)程度であれば十分収まる想定 |
| サインアップ要件 | クレジットカード等の支払い方法の登録が必須 |
| その他 | GPU/TPUは無料枠対象外(本用途では不要) |

### 2.1 OCIとの違いで特に注意すべき点

1. **リージョンが米国固定**: 日本のAPI(楽天・カーリル)への通信レイテンシはOCIの日本リージョン案より大きくなるが、1日1回のバッチ処理・手動でのWeb画面確認が主用途のため実用上の支障は無いと想定
2. **静的IPの課金条件がOCIと異なる**: GCPでは「実行中インスタンスに割り当てている間だけ無料」。停止すると課金が始まる。**VMを止めない運用**が無料を維持する条件になる(24時間稼働させる今回の設計とは相性が良く、意図せず停止しない限り問題はない)
3. **既定では動的(エフェメラル)IPが割り当てられる**ため、作成後に「静的IPに昇格(予約)」する一手間が必要(手順は5.2参照)

## 3. 推奨構成(OCI向けの計画から変更なし)

| 項目 | 推奨 | 理由 |
|---|---|---|
| インスタンスタイプ | **e2-micro**(GCP Always Freeで選べるのはこれのみ) | book-radarは軽量なNode.js+SQLiteアプリのため1GBメモリで十分 |
| OS | **Ubuntu 24.04 LTS** | Node.js公式セットアップ手順が豊富 |
| リージョン | `us-west1`/`us-central1`/`us-east1`のいずれか(無料枠対象は固定) | 特にこだわりが無ければ`us-west1`(オレゴン)が地理的に日本から比較的近い |
| Node.jsバージョン | v22以降(`node:sqlite`対応。現行開発はv24) | 既存コードとの整合性 |

## 4. book-radarの稼働方式(OCI向けの計画から変更なし)

1. **巡回バッチ**: `npm run check-new-releases` を**cron(例: 毎日朝6時)**で自動実行
2. **Web画面**: `npm run web` のNode.jsプロセスを**systemdサービス化**し、再起動しても自動復帰するようにする

### 4.1 Web画面への公開方針(確定・変更なし)

**SSHトンネル経由のみでアクセス**(ポートは外部に公開しない)。`ssh -L 3000:localhost:3000 <ユーザー名>@<VMの静的IP>` のようにトンネルしてからブラウザで`localhost:3000`を開く。認証機能を作らずに済み、最も安全。DBはVMに一元化する。

## 5. セットアップ手順

### 5.1 ユーザー側での作業(アカウント作成を伴うため代行不可)

1. https://cloud.google.com/free/ からサインアップ(クレジットカード登録が必要)。90日間$300分のクレジットが付く無料トライアルとは別に、Always Free枠は**トライアル終了後も恒久的に**e2-micro等が無料で使える
2. [Google Cloud Console](https://console.cloud.google.com/)でプロジェクトを作成(任意の名前でよい、例: book-radar)
3. 「Compute Engine」→「VMインスタンス」→「インスタスの作成」で以下を設定:
   - 名前: 任意(例: book-radar)
   - リージョン: `us-west1`(オレゴン)推奨、ゾーンは任意(例: `us-west1-b`)
   - マシンタイプ: **e2-micro**(Always Free対象と画面上に表示されるはず)
   - ブートディスク: Ubuntu 24.04 LTS、標準永続ディスク(pd-standard)、30GB以内
   - ファイアウォール: 「HTTPトラフィックを許可」等のチェックは**不要**(Web画面はSSHトンネル経由のみのため、ポートを外部公開しない)
4. 作成後、インスタンス一覧に表示される**外部IP**をクリックし、**「静的アドレスに昇格」**を選択(これをしないと再起動時にIPが変わってしまう)
5. SSH接続方法: Google Cloud ConsoleのVMインスタンス一覧から「SSH」ボタンでブラウザ上から接続できる(鍵ファイルの管理が不要で簡単)。私が代行する場合は、ブラウザSSHとは別に**通常のSSH鍵ペアを作成してVMに登録**する必要があるため、以下のいずれかの方法を取る:
   - a) Google Cloud Consoleの「メタデータ」→「SSH認証鍵」に、私が生成する公開鍵を登録していただく(このセッションで鍵ペアを生成してお渡しできる)
   - b) `gcloud compute ssh`用にユーザー側でgcloud CLIをセットアップし、認証情報一式を共有いただく(やや煩雑)
   - → **a)を推奨**。次のステップで公開鍵をお渡しする

### 5.2 私が代行できる作業(VMの外部IPとSSHアクセスを共有いただければ)

1. SSH接続確認
2. Node.js・gitのセットアップ
3. book-radarリポジトリのclone、`.env`の設置(APIキー類は改めて共有が必要)
4. `npm run check-new-releases`のcron登録
5. `npm run web`のsystemdサービス化
6. VMの静的IPを楽天ブックスAPIの許可リストに追加(これは楽天側の設定なのでユーザー側の操作が必要)

## 6. 未確定事項

- `library/.env`の各種シークレット(CALIL_APPKEY, RAKUTEN_APP_ID/SECRET, GOOGLE_BOOKS_API_KEY)をVMにどう安全に転送するか(SCP等で個別に転送する想定)
- SSH認証鍵の登録方法(5.1の4番、a案で進めるかの最終確認)

## 7. 安全策(2026-07-25追加)

無料枠の範囲で運用するにあたり、意図しない課金を防ぐための3つの安全策をまとめる。

### 7.1 課金予算アラートの設定手順

Google Cloud Consoleで、支出が一定額を超えたらメール通知が届くように設定できる。**無料枠内で運用している限り本来は0円のはずだが、設定ミスや無料枠対象外リソースの作成に気付くための保険として設定しておく。**

1. Google Cloud Consoleで「お支払い(Billing)」→「予算とアラート(Budgets & alerts)」を開く
2. 「予算を作成(Create budget)」をクリック
3. 予算の名前を入力(例: book-radar-budget)
4. 対象範囲(スコープ)は「プロジェクト」で、作成したプロジェクト(例: book-radar)を選択。期間は「月次」
5. 予算額を設定:
   - **注意**: 予算額は請求先アカウントの通貨(多くの場合USD)で入力する。日本円の請求先アカウントであれば「500」と直接入力できるが、USD建ての場合は500円相当のドル額(執筆時のレートで概ね3〜4ドル程度)を入力する。実際の請求先アカウントの通貨は「お支払い」画面の「概要」で確認できる
   - 「指定額(Specified amount)」を選択し、上記の金額を入力
6. しきい値ルールを設定(デフォルトの50%/90%/100%のままでよい。「実際のコスト(Actual)」ベースを選択)
7. 通知先: デフォルトで請求先アカウントの管理者にメール通知が届く設定になっている。追加のメールアドレスに送りたい場合はCloud Monitoringのアラート通知チャネルを別途設定する(必須ではない)
8. 「完了(Finish)」で保存

### 7.2 運用ルール: 「アップグレード」ボタンは絶対に押さない

GCPの無料トライアル($300分のクレジット、90日間)は、**明示的に「アップグレード」を選択して有料のお支払いアカウントに切り替えない限り、期限や上限に達しても自動的に課金されることはない**(Google公式ドキュメントで確認済み)。

- トライアルの90日が経過、または$300のクレジットを使い切った場合: **アップグレードしなければ、作成したリソースが停止するだけ**(自動課金は発生しない)
- 停止後30日間の猶予期間があり、その間にアップグレードすればリソースを復旧できる。猶予期間を過ぎるとリソースは完全に削除される
- **運用ルール**: Google Cloud Consoleやメールで「アップグレードのご案内」「無料トライアルが終了します」等の通知が来ても、**「アップグレード」「お支払いを有効にする」に類するボタンは絶対に押さない**。Always Free枠(e2-micro等)は無料トライアルとは別枠のため、トライアルが終了・停止してもAlways Free対象のリソースの扱いがどうなるかは実際にその状況になった際に改めて確認する(基本的にはAlways Free枠は請求先アカウントが有効である限り恒久的に無料と案内されているが、トライアル専用アカウントのまま放置した場合の正確な挙動は要注意)

### 7.3 VM削除時に静的IPも同時に解放する手順

**重要**: VMインスタンスを削除しても、それに割り当てていた静的IPアドレスの予約は自動的には解放されない。2章の通り、静的IPは「実行中インスタンスに割り当てている間のみ無料」なので、**VM削除後に静的IPを放置すると未使用予約扱いで課金が始まる**。万が一VMを削除する場合は、必ず以下の順で作業する。

1. Compute Engine →「VMインスタンス」で対象のインスタンスを選択し「削除」
2. 削除完了後、Compute Engine →「VPCネットワーク」→「IPアドレス」(または「予約済み静的アドレス」)を開く
3. 対象の静的IPアドレス(ステータスが「使用されていません(In use by: なし)」になっているはず)を選択し、「静的アドレスを解放(Release static address)」を実行
4. 解放後、一覧から当該IPが消えていることを確認する

→ VM削除と静的IP解放は**必ずセットで実施**し、削除後に放置しないこと。
