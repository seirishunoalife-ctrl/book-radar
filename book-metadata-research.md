# 書誌情報連携 API比較(楽天ブックスAPI vs openBD)

調査日: 2026-07-22

## 1. 比較サマリー

| 項目 | 楽天ブックス書籍検索API | openBD |
|---|---|---|
| エンドポイント | `https://app.rakuten.co.jp/services/api/BooksBook/Search/20170404` | `https://api.openbd.jp/v1/get?isbn=...` |
| 検索方法 | **ISBN検索・タイトル検索・著者名検索・出版社検索**(いずれか1つ以上必須) | **ISBNのみ**(カンマ区切りで複数指定可)。タイトル/著者名での検索は不可 |
| 表紙画像 | **あり**(`smallImageUrl`64x64 / `mediumImageUrl`128x128 / `largeImageUrl`200x200、いずれもhttps) | `summary.cover`フィールドはあるが**多くの書籍で空文字**(実機確認: 新潮文庫「こころ」で空)。出版社が用意していない場合は取得不可 |
| 取得できる主な情報 | title, titleKana, subTitle, seriesName, author, authorKana, publisherName, isbn, itemCaption(内容紹介), salesDate, itemPrice, itemUrl, availability, reviewCount, reviewAverage, chirayomiUrl 等 | isbn, title, volume, series, publisher, pubdate, cover, author(ローマ字表記なし、"姓,名,生没年"形式の1文字列)。加えて`hanmoto`オブジェクトに書評掲載情報等の付加データ |
| 網羅性 | 楽天ブックスで販売されている書籍のみ(比較的新しい書籍が中心) | JPO(出版情報登録センター)登録データが中心。実機確認したところ**比較的新しい書籍(コンビニ人間 `9784041040203`)がヒットしないケースもあった**一方、"こころ"のような古典は取得できた |
| 利用登録 | **要**: Rakuten Developersでアカウント作成の上、アプリケーションIDを自己発行(承認待ちなし、即時発行)。加えてアプリ側では`applicationId`パラメータのみ必須(アフィリエイトIDは任意) | **不要**(APIキーなし、誰でも即座に呼び出し可能。実機確認済み) |
| レート制限 | 公式ドキュメントに具体的な数値の記載なし。上限超過時は`HTTP 429`が返る旨の記載のみ | 明記なし。「書誌情報1件あたり1ミリ秒以下で応答」と高速性のみ言及。データの改変禁止・販促目的限定などの利用条件あり |
| レスポンス形式 | JSON/XML(`formatVersion=2`推奨) | JSON固定。ISBNごとに配列要素を返し、該当なしは`null`(実機確認済み: `[null]`) |

## 2. 実機確認結果(openBD、appkey不要のためすぐ検証可能)

```
GET https://api.openbd.jp/v1/get?isbn=9784101010137  (夏目漱石「こころ」)
→ summary: {isbn, title:"こころ", volume:"", series:"新潮文庫", publisher:"新潮社",
            pubdate:"200403", cover:"", author:"夏目,漱石,1867-1916"}
  ※ cover は空文字(表紙画像なし)

GET https://api.openbd.jp/v1/get?isbn=9784041040203  (コンビニ人間)
→ [null]  (openBDに登録なし、ヒットしない)
```

## 3. 結論・採用方針

引き継ぎドキュメント(5章)の当初決定どおり、**楽天ブックスAPIを書誌情報取得の主軸とする**。理由:

1. 本アプリの目的である「表紙表示」「タイトル検索(曖昧検索の第一歩)」の両方は、**楽天ブックスAPIでなければ実現できない**(openBDはISBN検索専用かつ表紙画像がほぼ空)
2. 楽天ブックスAPIのタイトル検索(`title`パラメータ)はキーワード的な部分一致検索であり、追加実装なしでそのまま「タイトルの一部入力での曖昧検索」の要件を満たせる

**openBDは補完として、以下の場合にフォールバック的に使う**:
- 楽天ブックスに登録されていない書籍(絶版・古い書籍など)のISBN検索時
- 将来的に書評・関連情報(`hanmoto`)を追加したくなった場合

### 3.1 未解決・要ユーザー対応(2026-07-22時点、下記4章の実機確認前の記述)

楽天ブックスAPIの利用には**アプリケーションIDの取得(Rakuten Developersでのアカウント登録)**が必要。カーリルのappkeyと同様、ログインを伴うため代行不可。取得後、`library/.env`に`RAKUTEN_APP_ID`として設定してもらう必要がある。

→ 実装はappkeyが無くても動作するように設計する(未設定時はopenBDのみで動作し、タイトル検索は「楽天APIキー未設定」の明示的なエラーメッセージを返す)。

## 4. 【重要・方針修正】楽天API認証の実機確認結果(2026-07-22)

`https://webservice.rakuten.co.jp/app/create` から実際にアプリを登録し、動作確認したところ、**当初の想定(承認不要・登録のみですぐ使える・IP制限なし)は誤りだった**ことが判明した。楽天側がAPI認証方式を刷新しており、現在は以下が必須になっている。

| 項目 | 当初の想定(誤り) | 実機確認結果(2026-07-22) |
|---|---|---|
| エンドポイントホスト | `app.rakuten.co.jp` | **`openapi.rakuten.co.jp`**(`app.rakuten.co.jp`は新形式の`applicationId`を受け付けず`wrong_parameter`エラーになる) |
| 必須パラメータ | `applicationId`のみ | **`applicationId` + `accessKey`の両方が必須**(`accessKey`が無いと`accessKey must be present`エラー) |
| `applicationId`の形式 | 長い数字列 | **UUID形式**(例: `a7f70123-01a2-4663-89c1-2cc47dbe8eb1`) |
| IP制限 | なし(公開API) | **あり**。登録したアプリの許可IPアドレスに、実際にAPIを呼び出すサーバーのIPを登録する必要がある(未登録の場合 `403 CLIENT_IP_NOT_ALLOWED`) |

**再現性の確認**: 一度目に発行された認証情報でも、`https://webservice.rakuten.co.jp/app/create`から改めて新規作成した認証情報でも、**同一の`applicationId`・同じIP制限ありの挙動**になることを確認した。これはRakuten Web Service全体が新方式(IP許可リスト必須)に統合済みであり、「IP制限なしの旧方式」はもはや選べない状態と考えられる。

### 4.1 アーキテクチャへの影響

これは単なる実装の修正では済まない、**ホスティング方針そのものに関わる制約**である。楽天ブックスAPIを呼び出す実行環境(巡回バッチ・API サーバーなど)は、**固定の送信元IPアドレスを持ち、そのIPをあらかじめRakutenアプリの許可リストに登録しておく必要がある**。

これにより、Vercel/Netlify等の一般的なサーバーレスPaaS(送信元IPが動的・不定)は追加の対策なしにはそのまま使えない。対応案は以下の3つ:

1. **常時稼働の固定IP VPSでアプリ全体(バッチ+将来のAPI)を動かす**(推奨・シンプル)。Oracle Cloud Infrastructure Always Free Tierのように永年無料枠で固定IPが得られるサービスや、さくらのVPS/ConoHa VPS等の低価格国内VPSが候補
2. **静的送信元IPを提供するプロキシ経由でRakuten APIのみ呼び出す**(QuotaGuard Static等)。フロントエンド/APIは自由な構成のまま維持できるが、構成要素が増える
3. **自宅サーバー + 固定IPオプション(ISP契約変更)**。低コストではない場合が多く、可用性もISP依存

→ 上記1(固定IP VPSで一本化)が個人開発向き・低コストの原則に最も合致すると考えられるため、次のホスティング検討ではこれを軸に進める。

## 5. 実機確認成功(2026-07-23)・原因判明

検証用IP(`106.153.89.185`)を許可リストに追加したにもかかわらず何度も`CLIENT_IP_NOT_ALLOWED`が続いたが、原因は**Rakutenダッシュボードに同名(「book radar」)のアプリが2つ存在し、IPを追加していたのが`.env`の`RAKUTEN_APP_ID`(`a7f70123-01a2-4663-89c1-2cc47dbe8eb1`)とは別のアプリID(`5fcdae94-9203-40b5-966d-a67805ab73b9`)だったため**。正しい方のアプリにIPを追加したところ即座に解消した。

**教訓**: 楽天ウェブサービスの登録操作を複数回試みると、アプリケーションURLが同じでも別アプリとして重複登録されることがある。設定変更時は「アプリ名」ではなく「アプリケーションID」で対象アプリを特定すること。

### 5.1 実装バグの発見・修正

実データでの検証により、`formatVersion=2`のレスポンス構造が公式ドキュメントの一般例と異なることが判明した。

- ドキュメント上の一般例: `{"items": [{...}]}` (小文字`items`)
- **実際のレスポンス(2026-07-23確認)**: `{"Items": [{...フィールドが直接...}]}` (**大文字始まりの`Items`**)

`src/adapters/rakutenBooksClient.ts`は`data.Items ?? data.items ?? []`で両対応するよう修正済み。

### 5.2 実データ確認結果

`npm run check-isbn -- 9784101010137` で以下を確認:
- タイトル「こころ」、著者「夏目 漱石」、出版社「新潮社」、発売日「2004年03月」
- 表紙画像URL(200x200): `https://thumbnail.image.rakuten.co.jp/@0_mall/book/cabinet/0137/9784101010137.jpg?_ex=200x200`
- `metadata_source: 'rakuten'`としてDBに保存済み

`npm run search-title -- "こころ"` でタイトル部分一致検索も正常動作(10件ヒット、表紙画像・ISBN・著者付きで一覧表示)。

書誌情報連携(楽天ブックスAPI優先・openBDフォールバック・タイトル曖昧検索)は実データで動作確認済み。残る課題はホスティング(4.1章)のみ。

-----

## 6. 【検証中】Google Books APIへの切り替え検証(2026-07-25)

### 背景

楽天ブックスAPIは4章のとおり固定送信元IPの許可リストが必須。Render等の動的IPホスティングでは使えないため、IP制限のないGoogle Books APIへの切り替えを検討する。

### 6.1 仕様調査結果

| 項目 | Google Books API v1 |
|---|---|
| エンドポイント | `https://www.googleapis.com/books/v1/volumes?q=...` |
| 検索方法 | `q`パラメータに`isbn:`/`intitle:`/`inauthor:`/`inpublisher:`等のフィールド指定が可能。組み合わせ検索も可(例: `q=flowers+inauthor:keyes`) |
| 利用登録 | Google Cloud ConsoleでAPIキーを作成するだけ(プロジェクト作成→「Books API」を有効化→認証情報でAPIキー発行)。**承認待ちの記載なし、自己発行**。IP許可リストの概念自体が無い(APIキー制限として任意でHTTPリファラ/IP/アプリを絞れるが必須ではない) |
| 匿名アクセス | ドキュメント上は「APIキーかOAuthトークンが必要」としつつ「公開データは可能」ともあり曖昧。**実機確認したところ匿名アクセスは事実上機能しない**(6.2参照) |
| レート制限 | 公式ドキュメントに具体的な数値の記載なし。Cloud Consoleでプロジェクトごとのクォータを確認・申請する方式 |
| レスポンス形式 | JSON。`items[].volumeInfo`に`title`, `authors`(配列), `publisher`, `publishedDate`, `industryIdentifiers`(ISBN_10/ISBN_13), `imageLinks`(`smallThumbnail`/`thumbnail`/`small`/`medium`/`large`/`extraLarge`の最大6段階), `infoLink`等 |

### 6.2 実機確認: 匿名アクセスは事実上使えない

```
GET https://www.googleapis.com/books/v1/volumes?q=isbn:9784101010137&country=JP  (APIキーなし)
→ HTTP 429
  "Quota exceeded for quota metric 'Queries' ... quota_limit_value: '0'"
```

匿名(APIキーなし)呼び出し用の共有クォータが**グローバルで割当0**になっており、実質的に機能しない。**実運用にはAPIキーが必須**と判断できる(ドキュメントの「匿名でも公開データは取得可能」という記載は実態と合っていない)。

### 6.3 実機確認: キー不要な代替エンドポイント(Google Books Dynamic Links API)での暫定調査

APIキー取得を待つ間、書誌詳細は取れないが「そのISBNをGoogleが認識しているか」だけ確認できる旧エンドポイントで暫定チェックした。

```
GET https://books.google.com/books?bibkeys=ISBN:9784101010137&jscmd=viewapi&format=json
→ ヒット(thumbnail_urlあり、ただしtitle/author等の書誌情報は含まれない)

GET https://books.google.com/books?bibkeys=ISBN:9784041040203&jscmd=viewapi&format=json  (コンビニ人間)
→ {} (ヒットなし)

GET https://books.google.com/books?bibkeys=ISBN:9784007610516&jscmd=viewapi&format=json  (夏目漱石「坊っちゃん」講談社学術文庫)
→ {} (ヒットなし)

GET https://books.google.com/books?bibkeys=ISBN:9784911449851&jscmd=viewapi&format=json  (絵本版「草枕」、比較的マイナー)
→ ヒット(thumbnail_urlあり)
```

**注意点**: このエンドポイントはGoogle Books本体のVolumes API v1とは別の古いAPIであり、索引・カバレッジが完全に一致するとは限らない。ただし「コンビニ人間」(芥川賞受賞の有名な現代小説ですらヒットしないのは懸念材料であり、Volumes API v1本体でも和書のカバー率に同様の課題がある可能性を示唆している。**この点はAPIキーによる本検証で確定させる必要がある。**

### 6.4 実装準備

比較検証用に`src/adapters/googleBooksClient.ts`を実装済み(`fetchByIsbn` / `searchByTitle` / `searchByAuthor`)。`BookInfo`の`source`に`"googlebooks"`を追加。まだ`bookInfoService.ts`には組み込んでおらず、比較検証専用。

### 6.5 実機確認結果(APIキー取得後、2026-07-25実施)

#### 6.5.1 「こころ」(9784101010137、ISBN検索)

```
GET .../volumes?q=isbn:9784101010137&country=JP&key=...
→ title: "こころ", authors: ["夏目漱石"], publisher: "Shinchosha/Tsai Fong Books",
  publishedDate: "2004-03", imageLinks: { smallThumbnail, thumbnail }のみ(medium/large/extraLargeなし)
```

タイトル・著者は正確。ただし**画像は`smallThumbnail`/`thumbnail`の2段階のみ**で、`medium`/`large`/`extraLarge`は返らなかった。実際にダウンロードして確認したところ`thumbnail`は約11KB程度の小さいJPEGで、楽天の`largeImageUrl`(200x200)より明らかに低解像度。

#### 6.5.2 「コンビニ人間」(9784041040203、村田沙耶香) — 【重大な問題を検出】(※7章で補足訂正あり)

> **7章での補足**: この`9784041040203`というISBNは、後の拡大検証で**楽天ブックスAPI・openBD双方も同じく「該当なし」を返す、実在しない(または現在は流通していない)ISBNだった**ことが判明した(記憶に基づいて設定した検証用ISBNが誤っていた)。正しいISBN(`9784167911300`、文春文庫版)ではGoogle Booksも正しく一致した(7.1参照)。以下の「重大な問題」は**「存在しないISBNを検索した際の挙動」としては引き続き有効な観察**だが、「実在するISBNに対して誤った本を返す」という意味では過大評価だった。詳細は7章を参照。

```
GET .../volumes?q=isbn:9784041040203&country=JP&key=...
→ title: "ヘンリーの想像する力", authors: ["スカイ・バーン"],
  industryIdentifiers: [{type:"ISBN_13", identifier:"9784041040201"}]  ← クエリと違うISBN!
```

**指定したISBN(`...0203`)と全く無関係の絵本(`ヘンリーの想像する力`)が返ってきた**。返却されたISBN(`...0201`)はクエリと1桁違いの別の本で、Google Books側が「該当ISBNなし」の代わりに近い数字のISBNを誤マッチさせたとみられる。`isbn:`検索は「無ければnull」ではなく**「違う本を返す」ことがある**、書誌情報連携として致命的なリスク。

タイトル+著者名(`intitle:コンビニ人間 inauthor:村田沙耶香`)で検索すると正しくヒットしたが、返ってきたISBN(`9784167911300`、文春文庫版)は**Rakuten側が返す版のISBN(`9784041040203`、角川文庫版)と異なる版**だった。本アプリはISBNをキーにDB保存・カーリルAPI照会を行う設計のため、版が食い違うと同一書籍として突合できない。

#### 6.5.3 著者名検索(`inauthor:夏目漱石`, `orderBy=newest`) — searchByAuthorの代替可否

```
totalItems: 300(参考: 楽天は実測30件/ページ、要ページング)
実際に返った20件のうち:
  - ISBN_13を持つもの: 8件(40%)。残り60%はISBNなしで本アプリのパイプラインでは使用不可
  - medium/large等の高解像度表紙画像を持つもの: 0件(0%)。全件thumbnail相当のみ
```

さらに`orderBy=newest`は本当の「新刊」だけでなく、大活字本シリーズ(2021年刊行の再編集版)のような**新刊ではない再発行版**を大量に上位表示しており、新刊検知用途としてはノイズが多い。

### 6.6 結論・推奨判断(初回検証時点。7章の拡大検証で一部修正した最終結論に置き換わる)

初回検証(「コンビニ人間」1件が誤ったISBNを返した事象)を根拠に、Google Books API不採用・楽天ブックスAPI維持を暫定的に推奨していた。ただしサンプル数が少なく、後述のとおりテストデータ自体に誤りがあったため、7章で6ジャンルに拡大して再検証した。**最終結論は7.3を参照。**

-----

## 7. 拡大検証: 6ジャンル×楽天/Google比較(2026-07-25)

「コンビニ人間」1件だけでは判断材料が不足しているとの指摘を受け、文学・話題作・ビジネス書・児童書・漫画・ライトノベルの6ジャンルで、`fetchByIsbn`の精度を横並び比較した。ISBNは全て**楽天ブックスAPIのタイトル検索で実在確認済みのもの**を基準値として使用した(前回検証で使った「コンビニ人間」のISBNは、実は楽天・openBD双方も認識しない誤ったISBNだったため、正しいISBNに差し替えた)。

### 7.1 ISBN検索(`fetchByIsbn`)結果

| ジャンル | 書籍 | 基準ISBN(楽天で実在確認済み) | 楽天の結果 | Google Booksの結果 |
|---|---|---|---|---|
| 文学(定番) | こころ / 夏目漱石 | 9784101010137 | ✅ 正確一致・表紙あり | ✅ 正確一致・表紙は低解像度サムネイルのみ |
| 話題作・受賞作 | コンビニ人間 / 村田沙耶香 | 9784167911300 | ✅ 正確一致・表紙あり | ✅ 正確一致・表紙は低解像度サムネイルのみ |
| ビジネス書・実用書 | 嫌われる勇気 / 岸見一郎・古賀史健 | 9784478025819 | ✅ 正確一致・表紙あり | ❌ `null`(該当なし。タイトル+著者名検索でヒットするが今度はISBNが無い。7.2参照) |
| 児童書・絵本 | ぐりとぐら / なかがわりえこ | 9784834000825 | ✅ 正確一致・表紙あり | ✅ 正確一致・表紙は低解像度サムネイルのみ |
| 漫画・コミックス | 鬼滅の刃 22 / 吾峠呼世晴 | 9784088824246 | ✅ 正確一致・表紙あり | ✅ 正確一致・表紙は低解像度サムネイルのみ |
| ライトノベル | ソードアート・オンライン1 アインクラッド / 川原礫 | 9784048677608 | ✅ 正確一致・表紙あり | ✅ 正確一致・表紙は低解像度サムネイルのみ |

**訂正**: 実在する正しいISBNで検索した場合、Google Booksが無関係の別の本を誤って返すケースは6件中0件だった。前回「重大な問題」として報告した誤マッチ(コンビニ人間・鬼滅の刃・ソードアート・オンライン)は、いずれも**私が検証用に用意したISBN自体が誤り(実在しないか流通していない番号)だった**ことが原因で、楽天・openBDも同じISBNには`null`を返していた。**「存在しないISBNを渡すと`null`ではなく無関係の本を返すことがある」という挙動自体は事実で今後も注意が必要だが、実在するISBNに対する誤マッチ率は今回の6件では0件だった。**この点は前回の報告が実態より悲観的だったため訂正する。

### 7.2 「嫌われる勇気」がヒットしなかった件の深掘り

`isbn:9784478025819`では`null`だったが、`intitle:嫌われる勇気 inauthor:岸見一郎`で再検索すると1件ヒットした。ただし**そのレコードにはISBNが一切含まれていなかった**(`industryIdentifiers`なし)。つまりGoogle Booksはこの本自体は認識しているが、書誌データとしてISBNを持たない不完全なレコードしかなく、ISBNベースの突合ができない。表紙画像は存在した(解像度は他と同様に低い)。

### 7.3 著者名検索(`inauthor:`)の追加検証

前回は夏目漱石のみだったが、現代作家(村田沙耶香)でも同様の傾向を確認した。

| 作家 | totalItems | 返却件数 | ISBN13あり | 割合 |
|---|---|---|---|---|
| 夏目漱石(前回) | 300 | 20 | 8 | 40% |
| 村田沙耶香(今回) | 300 | 20 | 6 | 30% |

2作家とも**返却された本の6〜7割にISBNが無く**、本アプリの新刊検知パイプライン(ISBN必須)ではそのまま使えないことを再確認した。古い作家・現代作家いずれでも同様の傾向であり、ジャンルや活動時期に依存する問題ではなく、**Google Books API全体の構造的な弱点**と考えられる。

表紙画像についても、6ジャンル・著者名検索含め検証した全ての書籍で`medium`/`large`/`extraLarge`は一度も返らず、`smallThumbnail`/`thumbnail`のみ(前回確認した約11KBの小さいJPEG相当)だった。**これは例外なく一貫していた**、最も再現性の高い弱点。

### 7.4 ジャンルごとの精度差について

今回の6ジャンルでは、**ジャンルによる精度差はほとんど見られなかった**。文学・話題作・児童書・漫画・ライトノベルの5ジャンルはISBN検索で正確に一致し、唯一「ビジネス書」の1件のみISBN無しでヒットした。ただしサンプルが各ジャンル1冊のみのため、「ビジネス書が弱い」と断定するには材料不足である。むしろ**ジャンルよりも「そのISBNをGoogle Books側がISBN付きレコードとして持っているか否か」という個別データ品質のばらつき**が支配的な要因と見られる。

### 7.5 Google Books APIが優れている点

- IP制限が無く、APIキーのみで動的IP環境(Render等)からでも利用できる(検証の主目的どおり)
- `inauthor`・`intitle`・`inpublisher`など柔軟なフィールド指定検索に対応
- ISBN検索で見つからない場合でも、タイトル+著者名検索であれば一定の確率で書籍自体は発見できる(ただしISBN無しのレコードのことがある)

### 7.6 最終結論(6.6を置き換え)

**Google Books APIへの全面切り替えは、依然として推奨しない。楽天ブックスAPIを維持し、固定IP確保(Oracle Cloud Free Tier等)で対応する方針を推奨する。**

拡大検証により「実在ISBNに対する誤マッチ」という最も深刻な懸念は事実上解消されたが、以下の2点は6ジャンル・2作家の検証を通じて一貫して再現され、実用上の弱点として残る。

1. **表紙画像の解像度不足**: 検証した全ての書籍で`smallThumbnail`/`thumbnail`(低解像度)のみ。`medium`以上は一度も取得できなかった。「表紙表示」を導入目的の一つとする本アプリにとって明確な後退
2. **著者名検索のISBN欠落率の高さ**: 新旧・ジャンルを問わず60〜70%の結果にISBNが無く、新刊検知パイプラインでそのまま使えない
3. (参考)**存在しないISBNに対して`null`ではなく無関係の本を返すことがある**という挙動も実機で複数回確認済みであり、ユーザーが誤入力したISBNを扱う`/isbn`画面等では注意が必要

一方で、実在するISBNに対する基本的なタイトル・著者の精度は十分実用的であり、**「ISBNが完全一致した場合のみ採用するガード付きの補完ソース」として、楽天ブックスAPIが持っていない書籍を拾う用途に限定して併用する価値はある**。ただし現状の優先度としては、書誌情報連携の主軸を差し替えるほどのメリットは無いと判断する。

→ **次のアクション**: 楽天ブックスAPI(現行構成)を維持しつつ、Renderの動的IP問題を解決するため、Oracle Cloud Infrastructure Always Free Tier等の固定IP確保手段の検討に進む(4.1章で既に候補として挙げていたもの)。

-----

## 8. 追加確認: 表紙画像の実ピクセルサイズ / ビジネス書の複数冊検証(2026-07-25)

### 8.1 表紙画像の実測ピクセルサイズ

実際に画像をダウンロードし、.NET `System.Drawing.Image`で実寸を計測した(スマホ本棚アプリの目安: リスト表示 幅100〜150px、詳細表示 幅250〜300px)。

| 書籍 | 楽天 large(`_ex=200x200`指定) | Google thumbnail(`zoom=1`) |
|---|---|---|
| こころ | 137×200 px | 128×182 px |
| ぐりとぐら(横長の絵本) | **200×145 px** | **128×92 px** |
| 鬼滅の刃22 | 126×200 px | 128×202 px |
| 生成AI最速仕事術 | (未計測) | 128×182 px |

**訂正**: 前回「低解像度」と表現したのは、ファイルサイズ(約11KB)からの印象が先行しすぎていた。実測すると、Google Booksの`thumbnail`は**横幅128pxで固定**(本の縦横比に応じて高さが変動)、楽天の`largeImageUrl`(`_ex=200x200`指定)は**長辺200pxに収まるよう可変**(縦長の本なら高さ200px、横長の本なら幅200px)という違いだった。

- 縦長(一般的な小説・新書等)の本では、楽天(137〜126px幅)とGoogle(128px幅)は**ほぼ同等**
- 横長の本(絵本等)では、楽天が幅200pxまで使うのに対しGoogleは128px幅に留まり、**楽天の方が明確に大きい**
- **どちらもリスト表示(100〜150px)には十分だが、詳細表示の目安(250〜300px)にはどちらも届いていない**。楽天がやや優位だが、両者とも「詳細表示用の高解像度画像」としては力不足である点は共通

なお`smallThumbnail`(zoom=5)と`thumbnail`(zoom=1)は、検証した書籍では**完全に同一のファイル**(同一バイト数)だった。Google Books側は多くの場合、実質1段階の解像度しか持っていないとみられる。

### 8.2 ビジネス書6冊での追加検証

「嫌われる勇気」1冊のみでは判断材料不足との指摘を受け、以下6冊で`fetchByIsbn`を比較した(ISBNは楽天のタイトル検索で実在確認済み)。

| 書籍 | 基準ISBN | 楽天 | Google Books(ISBN検索) |
|---|---|---|---|
| 完訳7つの習慣(2020版) | 9784863941014 | ✅正確一致 | ✅正確一致 |
| FACTFULNESS | 9784822289607 | ✅正確一致 | ✅正確一致 |
| 人を動かす 改訂文庫版 | 9784422101347 | ✅正確一致 | ✅正確一致 |
| LIFE SHIFT | 9784492533871 | ✅正確一致 | ✅正確一致 |
| 生成AI最速仕事術(2025年6月、直近1年以内) | 9784761278052 | ✅正確一致 | ✅正確一致 |
| ビジュアル 生成AIで爆速!ChatGPT仕事術(2025年9月、直近1年以内) | 9784296209316 | ✅正確一致 | ✅正確一致 |

**結果: 6冊中6冊、ISBN検索で正確に一致(表紙画像もすべてあり)。** 直近1年以内に出た新しいビジネス書2冊(生成AI関連)も含めて全て正確にヒットしており、**「嫌われる勇気」がISBN無しだった件はビジネス書ジャンル全体の傾向ではなく、その1レコード固有のデータ欠落だった**と結論できる。表紙画像サイズもここで抽出した1冊(生成AI最速仕事術)で128×182pxとなり、8.1の傾向と一致した。

### 8.3 最終判断の再確認

**8.1・8.2いずれも、7.6の最終結論(楽天ブックスAPI維持を推奨)を覆す材料にはならなかった。** ただし推奨の根拠はさらに次のように精緻化される。

- 表紙画像は「低解像度」というより「楽天がやや優位だが、どちらも詳細表示には力不足」という程度の差。深刻な後退というより**軽微〜中程度のマイナス**と再評価する
- ビジネス書は6冊中6冊正確にヒットし、直近の新刊もカバーしていた。「嫌われる勇気」の1件は個別のデータ欠落であり、ジャンル特性ではない
- 一方で7.3で確認した**著者名検索のISBN欠落率の高さ(60〜70%)**は今回のISBN単体検索の高い精度とは別の問題であり、こちらは複数作家で再現性高く確認されているため、引き続き有効な懸念点として残る

総合すると、**単一書籍のISBN検索精度という観点ではGoogle Books APIは楽天ブックスAPIとほぼ遜色ないレベルまで評価が上がった**が、(1)著者名検索のISBN欠落率の高さ(新刊検知パイプラインの要)、(2)表紙画像がやや小さめ、(3)無効ISBNに対して`null`を返さないことがある、という3点の弱点は変わらず残るため、**書誌情報連携の主軸を楽天ブックスAPIからGoogle Books APIへ切り替える判断は依然として推奨しない**。ホスティングの固定IP確保(Oracle Cloud等)で対応する方針を維持する。
